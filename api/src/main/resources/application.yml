server:
  servlet:
    context-path: /snackmachine
  netty:
    h2c-max-content-length:

logging:
  level:
    root: INFO

spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
    open-in-view: false
  h2:
    console:
      enabled: true

name: Snackmachine Test
